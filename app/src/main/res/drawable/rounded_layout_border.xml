<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/black"/> <!-- Background color -->
    <corners android:radius="60dp"/> <!-- Adjust the radius as needed -->
    <stroke
        android:width="2dp"
    android:color="@android:color/black"/> <!-- Border color -->
</shape>
